<h1>Edit <%= @product[:name] %></h1>
<%= link_to 'Back', product_path(@product) %> | <%= link_to 'Product Index', root_path %><br><br>
<%= render 'shared/errors', object: @product %>

<%= form_for(@product) do |f| %>

	<%= render 'form', f: f %>

<% end %>